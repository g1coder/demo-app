import {IFormValues} from './ui/form';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const sendFeedback = (values: IFormValues): Promise<void> => {
  return Promise.resolve();
};
