import {IFormValues} from '../ui/contact-form';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function sendFeedback(values: IFormValues): Promise<void> {
  return Promise.resolve();
}

export default {
  sendFeedback,
};
